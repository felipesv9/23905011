#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define Pi 3.14

int main() {
    float raio, area;
    double quadrado;
    
    printf("Digite o raio: ");
    scanf("%f", &raio);
    
    area = Pi * pow(raio, 2);
    
    printf("Area total do circulo: %.2f", area);
    
return 0;
}
